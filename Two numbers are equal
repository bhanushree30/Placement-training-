#include<studio.h>
void are equal subtraction (int a,int b)
{
  if((a-b)==0)
{
  print ("%d & %d are equal.\n",a,b);
}
else
{
printf("%d & %d are not equal.\n",a,b);
}
  int main ()
{
  int num1=7;
  int num2=7;
  int num3=12;
are equal substraction (num1,num2);
are equal substraction (num1,num3);
return 0;
}
  int
